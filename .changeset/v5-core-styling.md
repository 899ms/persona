---
"@runtypelabs/persona": minor
---

Opt into the v5 transcript, compact header, single-row composer, and panel defaults with `future.v5Defaults`. Add a transient connection status mode that renders above the composer. Existing v4 defaults remain unchanged, and explicit configuration and theme tokens continue to take precedence.
