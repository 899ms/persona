---
"@runtypelabs/persona": minor
---

Expose `getVisitorToken()` on the widget handle so trusted voice providers can read the current visitor credential and share a conversation with text, including after initialization claims and page reloads.
